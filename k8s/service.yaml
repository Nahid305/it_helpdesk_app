apiVersion: v1
kind: Service
metadata:
  name: it-helpdesk-service
  namespace: it-helpdesk
  labels:
    app: it-helpdesk
spec:
  type: ClusterIP
  ports:
  - port: 8501
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: it-helpdesk
